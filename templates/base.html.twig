<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}StreetGuard{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('assets/tailwind.css') }}">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
      /* Minimal fallback so text remains legible even if Tailwind fails to load */
      html { -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
      body { font-family: Inter, ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica Neue, Arial, 'Noto Sans', 'Apple Color Emoji', 'Segoe UI Emoji'; }
    </style>

    <meta name="mapbox-token" content="{{ mapbox_token }}">

    {# Page-level stylesheets (CDNs, fonts, inline styles) #}
    {% block stylesheets %}{% endblock %}
</head>
<body class="bg-black text-white font-sans">
    {% block body %}{% endblock %}

    {{ importmap('app') }}

    {# Page-level scripts (CDNs, inline JS) #}
    {% block javascripts %}{% endblock %}
</body>
</html>
