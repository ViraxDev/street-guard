<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{% endblock %} - StreetGuard</title>

    <link rel="stylesheet" href="{{ asset('assets/tailwind.css') }}">
    <link rel="icon" href="{{ asset('img/logo.ico') }}" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* Minimal fallback so text remains legible even if Tailwind fails to load */
        html { -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
        body { font-family: Inter, ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica Neue, Arial, 'Noto Sans', 'Apple Color Emoji', 'Segoe UI Emoji'; }
    </style>
</head>
<body class="bg-black text-white font-sans">

    <div class="relative min-h-screen flex items-center justify-center" style="font-family: Inter, ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica Neue, Arial, 'Noto Sans', 'Apple Color Emoji', 'Segoe UI Emoji';">
        {% include 'includes/_ambient_bg.html.twig' %}

        <nav class="fixed top-0 left-0 right-0 z-50 px-6 py-4">
        <div class="max-w-7xl mx-auto flex items-center justify-between">
            <a href="{{ path('app_home') }}" class="flex items-center space-x-3 hover:opacity-80 transition-opacity">
                <img src="{{ asset('img/logo.png') }}" alt="StreetGuard Logo" class="w-10 h-10 rounded-full">
                <span class="text-xl font-semibold">StreetGuard</span>
            </a>
        </div>
    </nav>

        {% block body %}{% endblock %}
        <div class="absolute inset-0 overflow-hidden pointer-events-none">
            <div class="particles">
                {% for i in 1..20 %}
                    <div class="particle"
                         style="
                                 left: {{ random(0, 100) }}%;
                                 width: {{ random(1, 3) }}px;
                                 height: {{ random(1, 3) }}px;
                                 animation-delay: {{ random(0, 10) }}s;
                                 animation-duration: {{ random(15, 25) }}s;
                                 "></div>
                {% endfor %}
            </div>
        </div>
    </div>

    {{ importmap('app') }}

    {% block javascripts %}{% endblock %}
</body>
</html>

